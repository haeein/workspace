#include <stdio.h>
/***
 * printf() -> 출력
 * scanf()->입력
 * ex) scanf("%d",&a);
 * -> 사용자가 입력한 값을 정수형으로 변환한 뒤 a 변수에 저장
 * 
 */
int main(){

    int a;
    scanf("%d\n",&a);
    printf("입력값: d\n",a);

}